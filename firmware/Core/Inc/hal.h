#ifndef HAL_H
#define HAL_H

#include <stdint.h>
#include <stddef.h>

// Shim for pqm4 library requirements
// The library seems to include this but might not use generic HAL functions 
// when compiled for specific target if we don't compile the tests.

#endif
